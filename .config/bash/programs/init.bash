# Setup 1password cli
if [ -f "$XDG_CONFIG_HOME/bash/programs/1password/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/1password/init.bash"
fi

# Setup ansible
if [ -f "$XDG_CONFIG_HOME/bash/programs/ansible/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/ansible/init.bash"
fi

# Setup asdf
if [ -f "$XDG_CONFIG_HOME/bash/programs/asdf/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/asdf/init.bash"
fi

# Setup bicep 
if [ -f "$XDG_CONFIG_HOME/bash/programs/bicep/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/bicep/init.bash"
fi

# Setup Brew
if [ -f "$XDG_CONFIG_HOME/bash/programs/brew/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/brew/init.bash"
fi

# Setup csharp 
if [ -f "$XDG_CONFIG_HOME/bash/programs/csharp/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/csharp/init.bash"
fi

# Setup docker_aws_azure
# if [ -f "$XDG_CONFIG_HOME/bash/programs/docker_aws_azure/init.bash" ]; then
#     . "$XDG_CONFIG_HOME/bash/programs/docker_aws_azure/init.bash"
# fi

# Setup git
if [ -f "$XDG_CONFIG_HOME/bash/programs/git/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/git/init.bash"
fi

# Setup kube
if [ -f "$XDG_CONFIG_HOME/bash/programs/kube/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/kube/init.bash"
fi

# Setup lazyGit 
if [ -f "$XDG_CONFIG_HOME/bash/programs/lazyGit/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/lazyGit/init.bash"
fi

# Setup npm
if [ -f "$XDG_CONFIG_HOME/bash/programs/npm/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/npm/init.bash"
fi

# Setup oh_my_posh
if [ -f "$XDG_CONFIG_HOME/bash/programs/oh_my_posh/startup.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/oh_my_posh/startup.bash"
fi

# Setup python
if [ -f "$XDG_CONFIG_HOME/bash/programs/python/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/python/init.bash"
fi

# Setup redis
if [ -f "$XDG_CONFIG_HOME/bash/programs/redis/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/redis/init.bash"
fi

# Setup tmux
if [ -f "$XDG_CONFIG_HOME/bash/programs/tmux/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/tmux/init.bash"
fi

# Setup vim
if [ -f "$XDG_CONFIG_HOME/bash/programs/vim/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/vim/init.bash"
fi

# Setup wget
if [ -f "$XDG_CONFIG_HOME/bash/programs/wget/init.bash" ]; then
    . "$XDG_CONFIG_HOME/bash/programs/wget/init.bash"
fi

